# 内容边界与脱敏说明

这个仓库是一份个人技术复盘，不是原工作系统的源码副本，也不能直接连接任何真实金融服务。

## 仓库包含什么

- 由我重新撰写的项目演进逻辑和技术取舍；
- 使用通用名称重新绘制的架构图与流程图；
- 不依赖真实接口的伪代码和合成 Case；
- 用于解释方法的阶段性规模与效果快照；
- 对实验口径、证据边界和不可直接比较结果的说明。

## 仓库不包含什么

- 公司名、内部产品名、组织信息和原始 Git 历史；
- 真实业务数据、客户数据、标注文件和完整评测集；
- 原始 System Prompt、Skill 正文、真实工具 Schema 与服务配置；
- 服务器路径、主机名、IP、端口、用户名和邮箱；
- API Key、Token、Cookie、Session、私钥或其他认证信息；
- 模型权重、LoRA Adapter、Checkpoint 和优化器状态；
- 可以直接复现或接入原系统的代码实现。

## 如何理解文中的数字

文中的样本量和 F1 是项目特定数据、Runtime、Skill 与 Scorer 组合下的阶段性快照，用于解释为什么技术路线发生变化。它们不是公共 Benchmark，也不表示当前线上长期水平。

不同章节若使用了不同数据集、模型版本或运行框架，会明确说明不能直接串成连续提升。没有逐项消融证据时，只解释整体机制，不给单个修改分配未经验证的收益。

## 通用化规则

- 真实工具动作改成 `SEARCH_PATH`、`EXPAND_PATH`、`FETCH_DATA` 等公开代称；
- 示例中的地区、主体、指标和值均重新编写；
- 内部任务名称统一改写为“指标检索、结构化表格、新闻、公告、研报”等任务形态；
- 只保留 MS-Swift、DeepSpeed、Slime、SGLang、verl、Megatron、vLLM、Claude Code SDK、OpenCode 等公开框架名称；
- 超参数以代表性实验配置呈现，不附启动路径、服务地址或原始配置文件。

## 安全检查

仓库上传前会检查常见凭据格式、内网地址、绝对路径、账号信息、二进制训练产物和原始数据后缀。自动扫描不能代替人工知识产权判断，因此原始工作目录与本仓库始终物理分离。
